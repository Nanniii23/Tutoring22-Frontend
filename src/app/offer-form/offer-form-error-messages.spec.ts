import { OfferFormErrorMessages } from './offer-form-error-messages';

describe('OfferFormErrorMessages', () => {
  it('should create an instance', () => {
    expect(new OfferFormErrorMessages()).toBeTruthy();
  });
});
